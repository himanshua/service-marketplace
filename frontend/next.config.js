/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // Add custom options here if you had them in next.config.ts
  // e.g. images: { domains: ['example.com'] }
};

module.exports = nextConfig;